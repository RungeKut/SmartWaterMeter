# SmartWaterMeter — Сводка по состоянию проекта

## Текущая архитектура

### Веб-сервер
- **ESPAsyncWebServer** + **WebSocket** — асинхронный, не блокирует `loop()`
- Логирование через `Log` (`src/Log.h`) — USB-Serial и Telnet одновременно
- **LittleFS** — SPA-фронтенд лежит в файловой системе (не в прошивке)
- **ArduinoJson 7.4.3** — структурированный обмен данными через WebSocket

### SPA-фронтенд (data/index.html)

Одна HTML-страница с тремя вкладками:

| Вкладка | Функции |
|---|---|
| **Dashboard** | Температуры (4 датчика), показания счётчиков (ГВС/ХВС), статус системы (WiFi, IP, uptime, heap, время) |
| **Settings** | WiFi, SMTP (email), расписание отчётов, начальные показания счётчиков, коэффициенты |
| **Calibrate** | Калибровка датчиков DS18B20: выбор датчика, нагрев, автоматическое определение |

- Material Design стили + Dark Mode
- WebSocket auto-reconnect
- Данные с датчиков и системная телеметрия обновляются каждые 2 секунды
- OTA-баннер с обратным отсчётом (5 мин на подтверждение прошивки)

### Сборка (актуальная)

| Параметр | Значение |
|---|---|
| RAM | 53.1% (43484 / 81920 байт) |
| Flash | 62.5% (652348 / 1044464 байт) |
| Платформа | ESP8266 (Wemos D1 mini), 80MHz |
| Статус | `pio run` — SUCCESS, 0 ошибок, 0 warning |

### Зависимости (lib_deps)

| Библиотека | Версия |
|---|---|
| paulstoffregen/OneWire | 2.3.8 |
| milesburton/DallasTemperature | 4.0.6 |
| NTPClient | 3.2.1 |
| me-no-dev/ESPAsyncTCP | 2.0.0 |
| me-no-dev/ESPAsyncWebServer | 3.6.0 |
| bblanchon/ArduinoJson | 7.4.3 |

---

## Последние изменения

### [14] fix: OTA никогда не применялся — флаг RTC затирал команду загрузчика (11.09.2026)

**Обнаружено при попытке прошить устройство по WiFi.**

`espota` отрапортовал `Result: OK`, плата перезагрузилась — но осталась на старой прошивке. Диагноз по Telnet-баннеру: дата сборки `Sep 9 2026 23:48:04`, в `help` нет команды `confirm`.

**Причина.** Пользовательская RTC-память ESP8266 — 512 байт, но первые 128 байт занимает команда загрузчика `eboot`, которую пишет `Updater::end()`. Цитата из ядра (`cores/esp8266/Esp.cpp`):

> *the eboot command will be stored into the first 128 bytes of user data... That means that user data present there will be lost*

Порядок в `ArduinoOTA::_runUpdate()` (`ArduinoOTA.cpp:315-333`):
1. `Update.end()` → пишет команду eboot `ACTION_COPY_RAW`
2. `_end_callback()` → `failsafe.updateFirmware()` → `rtcUserMemoryWrite(0, 0xDEAD0001)` — **затирает поле `magic` команды**
3. `ESP.restart()`

Загрузчик не находит валидной команды, образ из временной области не копируется в `0x00000`, плата поднимается на старой прошивке. Протокол OTA при этом отрабатывает штатно и рапортует успех.

**Следствие:** записи [11] и [12] о «успешном OTA» описывали передачу файла, а не применение прошивки. Устройство всё это время работало со сборкой от 9 сентября.

**Исправление:** `RTC_FLAG_OFFSET = 32` (байт 128) — флаг вынесен за пределы области eboot.

**Важно: это исправление нельзя доставить по воздуху.** Прошивка с багом при каждой попытке OTA снова затирает команду загрузчика. Нужна **одна прошивка по USB**:

```bash
pio run --target upload --upload-port COM3
pio run --target uploadfs --upload-port COM3
```

После неё OTA заработает нормально.

> Обновление файловой системы (`uploadfs`) багом не затронуто: без `ATOMIC_FS_UPDATE` образ LittleFS пишется напрямую, команда eboot не используется.

**Как проверять OTA впредь:** Telnet-баннер печатает `__DATE__ __TIME__` сборки. Не изменилась дата — образ не применился.

**Сборка:** SUCCESS, RAM 53.1% (43484 байт), Flash 62.5% (652348 байт).

### Проверено на железе (11.09.2026, 23:05-23:09)

**Прошивка по USB (COM3):** прошивка записана в `0x00000000`, LittleFS в `0x200000`, хеши сверены. Настройки EEPROM (SSID `75kV-2G`, показания счётчиков) пережили перепрошивку — `board_upload.erase_cmd =` работает.

**OTA после исправления — применяется.** Проверка по метке сборки в Telnet-баннере:

| Этап | Метка сборки |
|---|---|
| До вмешательства | `Sep  9 2026 23:48:04` |
| После USB-прошивки | `Sep 11 2026 23:05:32` |
| После OTA | `Sep 11 2026 23:08:35` |

Баннер подтверждения появился (`ota_pending=true`, `ota_remaining=287`), `/confirm` снял его (`ota_pending=false`), перезагрузки по таймауту не произошло.

**Что подтвердилось в работе:**

- **Логи в Telnet заработали.** Приходят строки прошивки: `[Telnet] Client 0 connected`, `[DS18B20] light rescan: 8 devices on bus`. Раньше это было невозможно
- **На шине 8 датчиков**, все 4 канала найдены и сопоставлены (Cold/Hot/Return/Supply, 25.7-26.1°C) — ровно тот случай, который ломало ограничение `i < NUM_SENSORS`
- `help` содержит `confirm`, `status` показывает IP только в STA-режиме
- Поле `ip` есть в `api.json` и `fullState`
- Пароли в `config` клиенту не уходят
- `/metrics` отдаёт корректные значения

**Остаётся сделать вручную:** показания счётчиков (`hot=10.000`, `cold=1.000`) накоплены старым кодом и завышены в 1000 раз — фактически 10 и 1 литр. Введите корректные значения в Settings → Meters.

### [13] fix: полный аудит проекта — 18 дефектов, документация приведена в соответствие (11.09.2026)

**Критичные ошибки:**

1. **Счётчики завышали показания в 1000 раз.** `MeterCounter::flush()` трактовал `litersPerPulse` как м³ на импульс, хотя поле подписано «литры» и в UI, и в `secrets.h`. Добавлено деление на 1000.
2. **Датчики за пределами первых четырёх не находились.** Циклы сопоставления в `begin()` и `loadExpectedAddrs()` были ограничены `i < NUM_SENSORS`. При 8 датчиках на шине исправный датчик показывал «Missing». Перебор идёт по `_allAddrsCount`.
3. **Мусорные дельты при калибровке** (`+536870900.0°`, `-1.5e+23°`). `loop()` вызывал `rescanBus()` *после* `startCalibration()`, и для новых индексов `_calibrateBaseTemp[]` оставался неинициализированным. Рескан перенесён внутрь `startCalibration()`, массив инициализируется целиком, добавлен флаг `_calibrateBaseReady`.
4. **Сохранение настроек стирало пароли.** Сервер не отдаёт пароли клиенту, поэтому поля в SPA всегда пустые — и любое сохранение записывало пустые строки. Добавлен `keepOrSet()`: пустое значение = «оставить как было».
5. **`rescanBus()` через сырой `_oneWire.search()`** нарушал собственное правило проекта (pitfalls §10) и ломал состояние DallasTemperature при каждом старте калибровки. Метод удалён.

**FailsafeOTA переписан** (решение принято после обсуждения):

Механизм A/B-слотов не соответствовал реальности — на ESP8266 их нет. `ArduinoOTA` пишет образ во временную область, а `eboot` при ребуте копирует его в `0x00000` поверх старого. Откатываться некуда.

- `getCurrentSlot()` всегда возвращал 1: маска `& 0x3FFFFF` по адресу в окне `0x402xxxxx` даёт значение заведомо больше `0x101000`
- `rollback()` писал `ACTION_LOAD_APP` на адрес, где образа не было
- Удалён `upload_script.py` — он шил прошивку в `0x10000`/`0x101000`, оставляя `0x0` пустым; плата бы не загрузилась

Осталось: флаг в RTC, баннер, `/confirm`, telnet `confirm`, одна перезагрузка по таймауту (вместо bootloop).

**Telnet реально начал показывать логи.** Хук `os_install_putc1()` перехватывал только вывод SDK, а `Serial.printf` шёл в UART мимо него — логи прошивки в Telnet не попадали никогда. Вдобавок предыдущий обработчик не сохранялся, и вывод SDK пропадал из USB-Serial. Добавлен `src/Log.h`: `Logger : public Print` пишет в Serial и в зарегистрированный приёмник. Весь проект переведён с `Serial.*` на `Log.*`.

**Dashboard перестал замирать.** Сообщение `sensors` несло только датчики, поэтому uptime, heap, время и IP обновлялись лишь при переподключении WebSocket. Введены `fillSystemState()` / `fillSensorState()` — оба сообщения собираются из общих helper'ов.

**Прочее:**
- Поле IP на Dashboard показывало данные наоборот: SSID при подключении к WiFi и `192.168.0.1` при его отсутствии. В `fullState` вообще не было поля `ip` — добавлено (и в `/api.json` тоже)
- Сообщение без `type` вызывало разыменование `nullptr` в `strcmp()`; `saveConfig` без объекта `config` записывал пустые строки во все поля — добавлены проверки
- `String((char*)data)` читал за пределами буфера WebSocket-кадра → `concat(data, len)`
- `ConfigStore::load()` печатал пароль WiFi открытым текстом в лог, видимый по Telnet
- `saveMeters()` побайтно повторял `save()`, и `loop()` вызывал обе подряд — удалён
- `smtpCallback()` был написан, но не зарегистрирован (`smtp.callback()`)
- `WiFi.RSSI()` отдавался в AP-режиме, где он бессмыслен
- Устаревший API ArduinoJson 5 (`createNestedObject`/`createNestedArray`) заменён на `to<JsonObject>()` / `add<JsonObject>()`
- Telnet: команда `confirm` отсутствовала в `help`; `status` печатал IP и AP-адрес всегда, включая `0.0.0.0`

**Проверено и оказалось НЕ проблемой:**
- Износ EEPROM от сохранения раз в 5 минут. `EEPROM.put()` в ядре сравнивает буфер через `memcmp` и ставит `_dirty` только при отличии, а `commit()` без изменений не трогает flash.

**Документация:** переписаны `modules/ota.md` и разделы 11-12 `pitfalls.md`, добавлены `modules/log.md` и разделы 13-15 pitfalls, исправлен адрес EEPROM (`0x7FB000` → `0x3FB000`, реальное значение из ld-скрипта), размер LittleFS (1 МБ → 2 МБ), интервал опроса (5000 → 2000 мс) в `architecture.md`, `websocket.md`, `calibration.md`, README.

**Сборка:** SUCCESS, RAM 53.1% (43484 байт), Flash 62.5% (652348 байт), предупреждений от кода проекта нет.

**Проверено на железе** — см. запись [14].

### [12] fix: OTA-тест — исправлен host_ip, добавлено правило FW (10.09.2026)

**Проблема:**
- `platformio.ini` содержал устаревший `--host_ip=192.168.88.113` (предыдущий IP хоста для portproxy)
- OTA падал с `Listen Failed` на несуществующем адресе
- После исправления IP — ошибка `No response from device` из-за блокировки порта 33777 Windows Firewall

**Исправления:**
- `platformio.ini`: `--host_ip` изменён с `192.168.88.113` на актуальный `192.168.88.92`
- На хосте добавлено правило Windows Firewall: `PlatformIO OTA 33777` (TCP in, порт 33777)

**Результаты OTA-теста:**
- Сборка: SUCCESS (RAM 53.1%, Flash 62.5%)
- OTA-upload: SUCCESS (28 сек, 656560 байт, `Result: OK`)
- ESP перезагрузка: SUCCESS (18 мс, TTL=255)
- Подтверждение прошивки (`/confirm`): SUCCESS
- OTA-прошивка работает напрямую (хост и ESP в одной подсети `/24`)

> **Устарело:** описанная здесь модель A/B-слотов неверна — на ESP8266 их нет. См. запись [13].

### [11] fix: Переписан FailsafeOTA — корректная адресация слотов, eboot, интеграция с ArduinoOTA (11.09.2026)

**Проблемы старой реализации:**
- `getCurrentSlot()` — брал адрес переменной в RAM (`0x3FFFxxxx`), всегда возвращал слот 1
- `rollback()` — неверные адреса: при slot=0 писал `0x100000` (начало slot B, а не slot A)
- Нет интеграции с ArduinoOTA — `onEnd()` не выставлял флаг в RTC, откат никогда не срабатывал
- Счётчик bootloop (3 retry) избыточен — достаточно одного retry для защиты

**Исправления в `FailsafeOTA.h`:**
- `getCurrentSlot()` использует адрес функции (IROM `0x402xxxxx`), маскирует `0x3FFFFF`, сравнивает с `SLOT_B_IMAGE_ADDR=0x101000`
- Корректные константы: `SLOT_A_IMAGE_ADDR=0x10000`, `SLOT_B_IMAGE_ADDR=0x101000`
- `rollback()` пишет eboot command на противоположный слот с правильным адресом
- Добавлен метод `updateFirmware()` — вызывается из `ArduinoOTA.onEnd()`, выставляет `RTC_MAGIC_FIRST_BOOT`
- Упрощена логика: два состояния (FIRST_BOOT → RETRY → rollback) вместо счётчика до 3

**Доработки в `Wemos_Mini.ino`:**
- `ArduinoOTA.onEnd()` вызывает `failsafe.updateFirmware()`
- Добавлен HTTP-эндпоинт `/confirm` для подтверждения прошивки из браузера

**Изменения в `platformio.ini`:**
- Раскомментированы `upload_protocol = espota` и `upload_port = 192.168.88.87`
- Добавлены `upload_flags` для совместимости с пробросом через netsh (раскомментировать при необходимости)

**Статус тестирования:**
- Сборка: SUCCESS (RAM 53.1%, Flash 62.5%)
- USB-прошивка: SUCCESS (залита через COM3)
- **OTA: SUCCESS** (см. [12] — протестирован: upload 28 сек, подтверждён через `/confirm`)
- Условие: хост и ESP в одной подсети; на хосте открыт порт 33777 (TCP in) в Windows Firewall

### [10] fix: интервал датчиков 5000 → 2000 мс
- Dashboard и Calibrate теперь получают данные с одинаковой частотой (~2 сек)
- 2000 мс безопасно: конверсия ~750 мс, ~1250 мс на WiFi/WebSocket

---

## Состояние оборудования

**Устройство:** `SmartWaterMeter-93C195` (MAC: e8:9f:6d:93:c1:95)
**IP:** `192.168.88.87`
**Ревизия платы:** Wemos D1 mini, ESP8266, 4MB Flash
**Последняя прошивка:** `Sep 11 2026 23:08:35` — по OTA, подтверждена через `/confirm`. Правки [13] и [14] на устройстве
**Датчиков на шине:** 8, все 4 канала сопоставлены
**Датчики DS18B20:** подключены, показывают ~28°C (комнатная температура)
**Счётчики:** горячая — 2.000 m³, холодная — 0.000 m³ — значение ГВС накоплено со старым коэффициентом (завышено в 1000 раз, фактически ~2 литра); после прошивки [13] введите корректные показания в Settings
**WiFi:** connected, RSSI -78..-79 dBm

## Инструкция по прошивке

### USB

```bash
pio run --target upload --upload-port COM3
pio run --target uploadfs --upload-port COM3   # если менялся index.html
```

### OTA (через WiFi)

```bash
pio run --target upload
```

> **Важно:** Хост и ESP должны быть в одной сети. На хосте должен быть открыт TCP-порт 33777 (входящие) в Windows Firewall. Если OTA не работает:
> 1. Проверьте `upload_flags --host_ip` в `platformio.ini` — должен быть актуальный IP хоста
> 2. Проверьте правило FW: `netsh advfirewall firewall show rule name="PlatformIO OTA 33777"`
> 3. Если правило отсутствует: `netsh advfirewall firewall add rule name="PlatformIO OTA 33777" dir=in action=allow protocol=TCP localport=33777`

### Первый запуск после USB

1. Плата загрузится с новым FailsafeOTA
2. Если **не было OTA** — баннер подтверждения НЕ появляется, всё работает сразу
3. Если был OTA — открой `http://192.168.88.87/confirm` в браузере или нажми Confirm на баннере